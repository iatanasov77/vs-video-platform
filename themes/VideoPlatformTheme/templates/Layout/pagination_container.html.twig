{% if  items.pageCount > 1 %}
    <div class="row">
        <!-- paginator -->
        <div class="col-12">
            <div class="section__paginator">
                <!-- amount -->
                <span class="section__paginator-pages">Showing {{ items.count }} of {{ items.getTotalItemCount }}</span>
                <!-- end amount -->

                {{ knp_pagination_render(items, 'Layout/pagination.html.twig') }}
            </div>
        </div>
        <!-- end paginator -->
    </div>
{% endif %}
